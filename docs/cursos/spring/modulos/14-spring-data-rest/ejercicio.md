---
sidebar_label: "Ejercicio"
---

## Ejercicio 3: Projection

Crea una proyección `TaskSummaryProjection` que exponga `id`, `title`, `status` y `userName`. Úsala con `?projection=summary`.

---

## Ejercicio 4: Evento HandleBeforeCreate

Implementa un `TaskEventHandler` que valide que el título de la tarea no esté vacío antes de crear, usando `@HandleBeforeCreate`.

---

## Ejercicio 5: HAL Explorer

Agrega la dependencia `spring-data-rest-hal-explorer` y explica cómo acceder al explorador y qué beneficios ofrece para probar la API.
